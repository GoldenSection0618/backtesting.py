# ============================================================
# constraints.txt — 课程复现环境版本约束
# ============================================================
# 上下文层：FractionalBacktest 测试在 pandas 3.x 下报只读数组错误，
#           这是上游兼容性问题，此处锁定 pandas<3 避开。
# 使用方式：pip install -r requirements.txt -c constraints.txt
pandas<3
