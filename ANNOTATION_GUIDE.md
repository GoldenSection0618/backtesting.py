# 中文注释规范

我在 Backtesting.py 源码里加注释的时候遵循下面几条规则。

## 注释要覆盖三个层次

大作业要求注释不能只写"这行干了什么", 还要从三个角度解释:

1. **功能层**: 这段代码做了什么。
2. **设计层**: 为什么这样写, 用了什么 Python 特性或设计模式。
3. **上下文层**: 这代码在整个回测框架里处于什么位置, 和哪些模块有关系。

## 怎么加注释

1. 简单语句用行内注释, 只解释非显而易见的点。
2. 类、函数、复杂逻辑用块注释概括整体作用。
3. 配置文件用 `#` 解释每项配置的含义。
4. JavaScript 用 `//` 注释。
5. 不写"功能层/设计层/上下文层"这种标签, 把三层意思自然地揉进注释里。

## 不能做的事

1. 不改函数名、类名、变量名、导入路径。
2. 不动回测核心逻辑 (Strategy.run / _Broker._process_orders 等)。
3. 不改 README、LICENSE、CSV 数据文件。
4. 中文注释不能放进字符串常量、正则表达式、HTML 模板、CSV 数据里。
5. 每改完一批文件就跑一次 `python run_demo.py` 确认没坏。
6. 注释的目的是帮助理解代码, 不能改变程序行为。
