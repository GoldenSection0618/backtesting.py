# 中文注释规范

## 注释层次

逐行或逐块注释覆盖三个层次:

1. 功能层: 代码做了什么, 输入输出是什么。
2. 设计层: 为什么这样写, 涉及哪些 Python 特性、设计模式或工程约定。
3. 上下文层: 该代码在回测框架中的位置, 与上下游模块的关系。

## 注释方式

- 简单语句用行内注释, 只写关键信息。
- 类、函数、逻辑块用块注释说明整体意图。
- 配置文件用 `#` 解释每项含义。
- JavaScript 文件用 `//` 注释。

## 约束

1. 不修改函数名、类名、变量名、导入路径。
2. 不改变核心回测逻辑。
3. 不修改 README、LICENSE、CSV 数据文件。
4. 中文注释不写入字符串、正则、HTML 模板、CSV 数据体内。
5. 每批注释后运行 `python run_demo.py` 验证。
