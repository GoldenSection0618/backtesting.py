# 中文注释规范

## 注释层次

逐行或逐块注释覆盖三个层次:

1. 功能层: 说明代码完成了什么操作, 输入和输出分别是什么。
2. 设计层: 说明为什么采用这种写法, 涉及哪些 Python 语言特性、设计模式或工程约定。
3. 上下文层: 说明该段代码在回测框架整体中的位置, 与上下游模块之间的调用或依赖关系。

## 注释方式

- 简单语句使用行内注释, 仅解释非显而易见的逻辑。
- 类、函数和复杂逻辑块使用前置块注释概括整体意图。
- 工程配置文件使用对应语法支持的注释符 (`#`, `//` 等) 解释每项配置的含义。
- JavaScript 文件使用 `//` 注释, 块注释使用 `/* */`。

## 约束

1. 不修改任何函数名、类名、变量名或导入路径。
2. 不改变核心回测逻辑, 包括 Strategy 的执行流程、_Broker 的撮合规则和 Backtest 的主循环结构。
3. 不对 README.md, LICENSE.md 以及 CSV 数据文件进行原地注释。
4. 中文注释不写入字符串常量、正则表达式、HTML 模板及 CSV 数据体内, 避免破坏运行时语义或数据解析。
5. 每完成一批文件的注释修改后, 运行 `python run_demo.py` 确认功能未被破坏。
