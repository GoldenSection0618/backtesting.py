# 中文注释规范

本项目按照《Python 高级程序设计》大作业要求，对 Backtesting.py 精简工程进行中文注释。

## 注释层次

1. 功能层：解释代码做了什么。
2. 设计层：解释为什么这样写，涉及哪些 Python 特性、设计模式或工程约定。
3. 上下文层：解释该代码在整个回测框架中的位置和作用。

## 注释方式

1. 简单语句使用简洁行内注释。
2. 类、函数、复杂逻辑块使用块注释或 docstring 说明整体作用。
3. 对核心算法和关键流程，先写块说明，再补充关键行注释。
4. 配置文件使用其对应语法允许的注释方式解释每项配置。
5. JavaScript 文件使用 `//` 注释。

## 安全规则

1. 不修改函数名、类名、变量语义和导入路径。
2. 不改变核心回测逻辑。
3. 不修改 README、LICENSE、CSV。
4. 不把中文注解放入字符串、正则、HTML 片段、CSV 数据体内。
5. 每批注释后必须运行 `python run_demo.py`。
6. 注释可以提升可读性，但不能改变程序行为。
