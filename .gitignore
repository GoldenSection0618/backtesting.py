# ============================================================
# .gitignore — Git 忽略规则配置
# 上下文层：定义哪些文件/目录不应纳入版本控制。
#           包括编译产物、打包输出、IDE 配置、测试覆盖率报告等。
# 设计层：使用通配符模式匹配，Git 原生支持的忽略机制。
# ============================================================

# --- Python 编译产物 ---
# 功能层：忽略 .pyc / .pyo / .pyd 字节码文件，避免将编译缓存提交到仓库
*.py[cod]

# --- 运行产物 ---
# 功能层：忽略 demo 运行生成的 HTML 图表和图片文件
*.html
*.png

# --- 自动生成的版本文件 ---
# 上下文层：_version.py 由 setuptools_scm 在安装时自动生成，不应手动编辑或提交
_version.py

# --- 打包构建产物 ---
# 功能层：忽略 setuptools 打包产生的 egg-info、eggs、dist 目录
*.egg-info
.eggs/*
__pycache__/*
dist/*

# --- 测试覆盖率报告 ---
# 上下文层：coverage.py 工具生成的覆盖率数据和 HTML 报告
.coverage
.coverage.*
htmlcov/*

# --- 文档构建产物 ---
# 上下文层：文档站构建输出，已从本课程分支删除 doc/ 目录
doc/build/*
build/*

# --- IDE 配置 ---
# 功能层：忽略 JetBrains IDE (.idea) 和 VSCode (.vscode) 的项目配置
.idea/*
.vscode/

# --- Jupyter Notebook 检查点 ---
# 功能层：Jupyter 自动生成的检查点目录
**/.ipynb_checkpoints

# --- 编辑器临时文件 ---
# 功能层：忽略编辑器自动保存的备份文件（以 ~ 结尾）
*~*

# --- 虚拟环境 ---
# 功能层：Python 虚拟环境目录，不应提交到仓库
.venv/
